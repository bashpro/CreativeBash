//- If statements

//- which layout/components are we working on?

if layout == "layout" 
  //layout dev view
  include layout 
  if website.title == ""
    p does your website have a name?

else if layout == "sizes"
  //testing sizing
  include sizes
  
else if layout == "main"
  //Main content
  include ../content/main

if website.plugins.jquery == "yes"
  - var jquery = "true"

if website.object.testing == website.test
  .section
    .box
      p WELCOME TO FRESH MANGO
      p #{website.array.key[0]}
  


if website.title !== ""
  .section
    .box
      p testing this because im curious
      
    .box
      p #{website.title}
